export function runCommand(command) {
  // Interpret command or send to backend shell
  // Return output and/or error as string or array of lines
}

export function clearTerminal() {
  // Clear the terminal output area
}

export function focusTerminal() {
  // Focus the input field for quick typing
}

export function addCommandToHistory(command) {
  // Store command in session history
}