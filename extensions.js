export function listAvailableExtensions() {
  // Get list of extensions from marketplace
}

export function installExtension(extensionId) {
  // Download and install extension
}

export function enableExtension(extensionId) {
  // Enable in current session
}

export function disableExtension(extensionId) {
  // Disable in current session
}